回答很详细，还有几个细节需要确认：

---

## 遗漏的问题

**Q20 你没回答：锁定后便签列表怎么显示？**

- A: 显示空列表 + 提示"已锁定，点击输入密码"
- B: 显示模糊的便签预览（看不清内容）
- C: 不显示任何内容，只显示密码输入框
答：C: 不显示任何内容，只显示密码输入框

**Q19 超时未操作和失去焦点：**

你标注了「已标注」，但这两个场景没有明确：

| 场景 | 锁定？ |
|------|-------|
| 应用失去焦点（切换到其他应用） | ？ |
| 超时未操作（如 5 分钟） | ？ |
答：这几个不锁定

---

## 需要细化的细节

### 时间轴 — 字段变化记录

你说要记录"标题从 AAA 改为 BBB"，具体记录哪些字段的变化？

| 字段 | 记录变化？ |
|------|-----------|
| 标题 | ✓ |
| 内容 | ？（内容可能很长，变化怎么展示？） |
| 颜色 | ？ |
| 分类 | ？（从 A 分类移到 B 分类） |
| 附件 | ？（添加了 xxx.pdf / 删除了 xxx.pdf） |
| 置顶 | ？ |
答：内容 只展示修改了的部分内容，颜色不处理、分类记录、颜色不记录、附件记录、置顶记录

**内容变化怎么展示？**

```
方案 A：只显示"内容已修改"
  10:30  修改了「会议记录」
         内容: 已修改

方案 B：显示前 N 个字符
  10:30  修改了「会议记录」
         内容: "今天讨论了..." → "今天讨论了项目进度..."

方案 C：显示字数变化
  10:30  修改了「会议记录」
         内容: 120字 → 256字
```
答：方案 A：只显示"内容已修改"
  10:30  修改了「会议记录」
         内容: 已修改

### 待办视图 — 排序

待办项平铺后，排序规则是什么？

| 排序方式 | 说明 |
|---------|------|
| 按所属便签分组 | 同一个便签的待办项放在一起 |
| 按创建时间 | 最新创建的在前 |
| 按完成状态 | 未完成在上，已完成在下 |
| 混合 | 未完成在上（按创建时间），已完成在下（按完成时间） |

**我的建议**：未完成在上（按创建时间倒序），已完成在下（按完成时间倒序）
答：未完成在上（按创建时间倒序），已完成在下（按完成时间倒序）

### 待办视图 — 隐私分类的待办

用户在「隐私」分类下有待办项，全局待办视图会显示吗？

- A: 显示（但需要输入密码才能看到内容）
- B: 不显示（隐私分类的待办完全隐藏）
- C: 显示为「🔒 隐私待办」，点击需要密码
答：B: 不显示（隐私分类的待办完全隐藏）

### 隐私分类 — 安全问题

你说用安全问题找回密码，那：

**Q: 安全问题设置几个？**

- A: 1 个（如：你的第一只宠物叫什么？）
- B: 2 个
- C: 用户自定义问题
答： B: 2 个

**Q: 安全问题答案需要验证吗？**

- A: 直接用答案重置密码
- B: 答案正确后才能重置
答：B: 答案正确后才能重置

### 隐私分类 — 同步的矛盾

你说隐私分类同步到云端且明文存储。这意味着：

- 云端数据库里可以看到隐私便签的内容
- 如果服务器被攻破，隐私数据泄露

**这和「隐私」的定位矛盾**。建议：

- A: 不同步（最安全，隐私数据只在本地）
- B: 同步但加密（云端存密文，密码是解密密钥）
- C: 同步明文，但在 UI 上提示用户"隐私分类数据会同步到云端"

你倾向哪个？
答：C: 同步明文，但在 UI 上提示用户"隐私分类数据会同步到云端"

---

以上这些确认后，三个功能的设计就完整了。